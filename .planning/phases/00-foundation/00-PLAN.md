# Phase 0 Plan — Foundation & Stabilization

**Phase:** 00-foundation
**Granularity:** Standard
**Mode:** YOLO (auto-approve checks, verifier, and roadmap approval)
**Created:** 2026-04-01

---

## Frontmatter

```yaml
# Coordination document — no wave/depends_on (sub-plans carry those)
files_modified: []
```

---

## Phase Goal

Stabilize the brownfield baseline before Phase 1 adds new data sources. Every plan must produce a tangible artifact (file, table, workflow) that can be verified.

**must_haves:**
1. `.planning/codebase/AUDIT.md` exists and lists all OpenAQ-specific hardcoded names, ClickHouse table schemas, and DAG task dependencies
2. `docker compose ps` shows all services healthy with resource limits applied
3. `git push` to a PR triggers lint + dbt CI pipeline that fails on intentional violations
4. AQICN-only pipeline runs for 7 consecutive days with 100% DAG success rate
5. `ingestion.control` table exists and is updated by `dag_ingest_hourly` after every run

---

## Wave Map

| Wave | Plans | Dependency |
|------|-------|-----------|
| 1 | 0.1 Audit, 0.2 Docker | None |
| 2 | 0.3 CI Pipeline | After 0.1 + 0.2 |
| 3 | 0.4 Stability Baseline | After 0.3 |
| 4 | 0.5 Ingestion Control | After 0.2 |

---

## Sub-plans

| Plan | File | Wave | Key Output |
|------|------|------|------------|
| 0.1 Audit | `plans/01-AUDIT.md` | 1 | `.planning/codebase/AUDIT.md` |
| 0.2 Docker | `plans/02-DOCKER.md` | 1 | `docker-compose.yml` hardened, README updated |
| 0.3 CI | `plans/03-CI.md` | 2 | `.github/workflows/ci.yml`, `.sqlfluff`, `.ruff.toml` |
| 0.4 Baseline | `plans/04-BASELINE.md` | 3 | `.planning/codebase/BASELINE-METRICS.md` |
| 0.5 Control | `plans/05-CONTROL.md` | 4 | `ingestion.control` table, DAG updates |

---

## Key Decisions Already Resolved

### D-09/D-10 (DB Name)
DB is consistently `air_quality` across all configs. `airflow/dags/dag_ingest_hourly.py` fallback changed to `'air_quality'` (fix in Plan 0.1, task `audit-db-name`). **No rename needed.**

### D-20 (airflow-webserver Limits)
`airflow-webserver` has no `mem_limit` — current usage is acceptable. **Do NOT add a limit.**

### CONCERN #7 (MergeTree Dedup)
`raw_openaq_measurements` uses `MergeTree()` with Python-side dedup only. Risk documented in AUDIT. **Fix deferred to Phase 2 Plan 2.4.**

### airflow-webserver Healthcheck
Already exists in `docker-compose.yml` at lines 111–115. **Do not duplicate.**

---

## Quality Gate Checklist

- [ ] `.planning/codebase/AUDIT.md` exists with all deliverables (Phase 1 scope)
- [ ] `docker-compose.yml` has `mem_limit` on 5 services (not airflow-webserver)
- [ ] `docker-compose.yml` has `healthcheck:` on 6 services total
- [ ] `.github/workflows/ci.yml` exists and is valid YAML
- [ ] `ruff==0.11.0` added to `requirements.txt`
- [ ] `.sqlfluff` and `.ruff.toml` created
- [ ] `ci` target in `dbt/dbt_tranform/profiles.yml`
- [ ] `docker-compose.test.yml` created
- [ ] `ingestion.control` table created in `scripts/init-clickhouse.sql`
- [ ] `python_jobs/common/ingestion_control.py` created
- [ ] `dag_ingest_hourly` updated with control tasks
- [ ] `dag_transform` updated with control task
- [ ] `airflow/dags/dag_ingest_hourly.py` fallback DB name fixed to `'air_quality'`

---

*Generated by: gsd:quick*
